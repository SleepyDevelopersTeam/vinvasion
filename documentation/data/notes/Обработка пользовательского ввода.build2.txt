Обработка пользовательского ввода

`[Описание проблемы]
События клавиатуры и мыши будут доставаться из соответствующих очередей
класса `.Input` в потоке обновления логики экземплярами класса `.Screen`.
При этом планируется, что они будут тут же и обрабатываться этими
объектами, так как сразу после получения события ссылка на него удаляется
из очереди событий в `.Input`. События поступают в очередь через
соответствующие методы класса `.Input`. Кладёт события в очередь главная
форма `.MainFrame`.

`@
`{::label}MainFrame`
`{::label}Input`
`{::label}Screen`
`{::label}Основные потоки программы`