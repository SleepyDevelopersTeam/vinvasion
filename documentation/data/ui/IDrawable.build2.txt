ru.sdevteam.vinv.ui.IDrawable

`[Общие сведения]
`(Java
public interface IDrawable
{
	...
}
`)

Интерфейс является родителем всех классов, которые можно отрисовать.

`[Объявляемые методы]
`(Java
public void paint(Graphics g);
`)

`@
`{::label}Sprite` (как пример реализующего класса)
`{::label}IUpdatable`
`{::label}Основные потоки программы`